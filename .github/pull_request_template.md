## What changed

- Brief description of the change.

## Bundle impact

- [ ] I ran `npm run build && npm run bundle:check`
- [ ] No main chunk is over the **350 KB gzipped** budget
- [ ] The bundle did not grow significantly for the value added

## Quality checks

- [ ] `npm run lint` passes
- [ ] `npm run test` passes
- [ ] `npm run check:circular` passes
- [ ] `npm run size-check` passes
- [ ] `npm run check:colors` passes

## Notes

- Any known trade-offs, follow-ups, or decisions to call out.
